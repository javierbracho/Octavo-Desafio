
</nav> <a href="/session/logout">Logout</a>


<div class="d-flex flex-row">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">
                        <h5 class="card-title">Carrito</h5>
                    </div>
                    <div class="card-body">
                        {{#each products}}
                        <div class="card mb-4">
                            <div class="card-body">
                                <h5 class="card-title text-dark">{{productName}}</h5>
                                <hr>
                                <p class="card-text">Precio: ${{price}}</p>
                                <p class="card-text">Cantidad: {{quantity}}</p>
                                <button class="btn btn-danger delete-product-btn" data-cart-id="{{cartId}}" data-product-id="{{productId}}">Eliminar</button>
                            </div>
                        </div>
                        {{/each}}
                        <div class="d-flex justify-content-center align-items-center">
                            <button id="vaciar-carrito-btn" class="btn btn-warning" data-cart-id="{{cartId}}">Vaciar Carrito</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    {{#if products.length}}
    <div class="container">
        <div class="d-flex justify-content-center align-items-center mt-4">
            <form action="/api/carts/{{cartId}}/purchase" method="post">
                <button type="submit" class="btn btn-primary">Checkout</button>
            </form>
        </div>
    </div>
    {{/if}}

<script src="/js/cart.js"></script>
